{
  "private": true,
  "name": "magic",
  "scripts": {
    "tst:init": "tst init --install false",
    "tst:scripts": "tst scripts",
    "tst:deps": "tst deps",
    "api:test": "tst test api",
    "api:test:watch": "tst test api --watch",
    "api:dev": "NODE_ENV=dev tst dev api",
    "api:build": "tst build api",
    "tag-editor:test": "tst test tag-editor",
    "tag-editor:test:watch": "tst test tag-editor --watch",
    "tag-editor:dev": "lerna run dev --stream --scope tag-editor",
    "tag-editor:build": "tst build tag-editor",
    "ui:test": "tst test ui",
    "ui:test:watch": "tst test ui --watch",
    "ui:generate": "lerna run generate --stream --scope ui",
    "ui:dev": "lerna run dev --stream --scope ui",
    "ui:build": "tst build ui",
    "api:backup": "lerna run backup --stream --scope api"
  },
  "devDependencies": {
    "@types/jest": "^23.3.2",
    "@types/node": "^10.11.3",
    "firebase-tools": "^6.1.2",
    "jest": "^23.6.0",
    "lerna": "^3.4.0",
    "nodemon": "^1.18.4",
    "parcel-bundler": "^1.10.3",
    "parcel-plugin-typescript": "^1.0.0",
    "ts-jest": "^23.10.3",
    "ts-node": "^7.0.1",
    "tsconfig-paths": "^3.6.0",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "twilio": "^3.19.2",
    "typescript": "^3.1.1",
    "typescript-tooling": "^4.1.0-alpha.01ab903"
  }
}
